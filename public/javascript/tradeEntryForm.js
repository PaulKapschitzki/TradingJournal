$(document).ready(() => {
    $('#cancelTradeEntry').on('click', () => {
        window.close();
    });

    // $('#submitTrade').on('click', () => {
    //     window.close(
    //         '/api/trades/create'
    //     );
    // });
});